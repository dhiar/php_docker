version: '3.8'

services:
  website:
    image: php:8.1-fpm
    volumes:
      - ./:/var/www/html
    working_dir: /var/www/html
  
  nginx:
    image: nginx:1.21.6-alpine
    volumes:
      - ./:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80